# STT 内容记录功能说明

## 功能概述

STT（Speech-To-Text）内容记录功能会自动记录每次监听会话中所有的语音识别文本，并保存到本地文件中。你可以通过Web界面实时查看或浏览历史记录。

## 主要特点

### ✅ 自动记录
- 启动监听时自动创建新会话
- 每条识别的文本实时写入文件
- 停止监听时自动保存并归档

### ✅ 实时保存
- 文本立即写入磁盘（不在内存中缓存）
- 即使程序异常退出，已识别的内容也不会丢失
- 使用文件缓冲刷新确保数据安全

### ✅ 智能去重
- 自动检测重复的递进式识别结果
- 合并同一句话的多次识别（如：50%→60%→70%→100%）
- 只保留最终完整的识别结果，避免冗余
- 可配置去重敏感度（默认阈值：0.6）

### ✅ 多种查看方式
1. **实时文本查看** - 边监听边查看，每3秒自动刷新
2. **历史记录查看** - 浏览所有历史会话，查看详情
3. **本地文件访问** - 直接打开 `.txt` 文件查看

## 文件结构

```
stt_records/
├── index.json              # 会话索引文件
├── 20260106_143025.txt     # 会话文本文件
├── 20260106_143025.json    # 会话详细数据
├── 20260106_150830.txt
├── 20260106_150830.json
└── ...
```

### 文本文件格式 (.txt)

```
====================================
STT 记录会话
会话ID: 20260106_143025
开始时间: 2026-01-06 14:30:25
====================================

[14:30:28] 大家好，今天我们开始上课
[14:30:35] 请同学们打开课本第五页
[14:30:42] 今天要讲的内容是数据结构
[14:31:05] 首先我们来看看栈的概念
...

====================================
结束时间: 2026-01-06 15:25:13
====================================
```

### JSON文件格式 (.json)

包含完整的结构化数据：
- 会话元信息（ID、时间、时长）
- 所有文本记录（带精确时间戳）
- 检测到的关键词记录
- 统计信息（字符数、文本条数等）

## 使用指南

### 1. 实时文本查看

**访问地址**: http://localhost:8080 → 点击 "📄 实时文本查看"

**功能**:
- 🔴 显示当前正在记录的会话
- 🔄 每3秒自动刷新内容
- 📊 实时统计（行数、字符数）
- 💾 一键下载当前文本
- 🗑️ 清空显示（不影响文件）
- ⏸️ 暂停/启动自动刷新

**适用场景**:
- 实时监控识别效果
- 检查是否正常工作
- 随时导出当前内容

### 2. 历史记录查看

**访问地址**: http://localhost:8080 → 点击 "📝 历史记录"

**功能**:
- 📋 查看所有会话列表（按时间倒序）
- 🔍 查看会话详情（完整文本+关键词）
- 💾 导出为文本文件
- 🗑️ 删除不需要的记录

**适用场景**:
- 复习以前的课程内容
- 查找特定时间的记录
- 管理存储空间

### 3. 本地文件访问

**文件位置**: 项目目录下的 `stt_records/` 文件夹

**使用方式**:
- 直接用记事本/VS Code等打开 `.txt` 文件
- 文件名格式: `YYYYMMDD_HHMMSS.txt`（如 `20260106_143025.txt`）
- 可以用文件资源管理器搜索、复制、备份

**适用场景**:
- 离线查看
- 批量备份
- 文本分析/处理

## 配置选项

### 启用/禁用记录

在 `config.json` 中设置：
```json
{
  "enable_stt_recording": true  // true=启用，false=禁用
}
```

**注意**: 禁用后重启系统才会生效。

### 配置去重阈值

在 `config.json` 中设置：
```json
{
  "stt_dedup_threshold": 0.6  // 去重相似度阈值（0-1）
}
```

**阈值说明**:
- **0.6（推荐）**: 平衡去重效果，适合大多数场景
- **0.5**: 更激进的去重，可能合并相似但不完全相同的内容
- **0.7**: 更保守的去重，只合并高度重复的内容
- **0.8-0.9**: 非常保守，几乎不去重
- **0.0**: 关闭去重（不推荐）

**工作原理**:
当新识别的文本与上一条文本的相似度 ≥ 阈值时，系统会更新上一条记录而不是新增一条。例如：
- 输入: "今天我们" → "今天我们要讲" → "今天我们要讲数据结构"
- 结果: 只保留 "今天我们要讲数据结构"（最终完整版本）

### 更改保存目录

在代码中修改（不建议）：
```python
# monitor_web.py
self.stt_recorder = STTRecorder(save_dir="stt_records")  # 改为其他目录
```

## 数据管理

### 存储空间

- 纯文本文件，占用空间很小
- 开启去重后，文件大小约减少 60-80%
- 一小时课程约 2-5 KB（开启去重）
- JSON 文件稍大（包含时间戳等元数据）

### 备份建议

定期备份 `stt_records/` 整个文件夹：
```bash
# 复制到备份目录
Copy-Item -Path "stt_records" -Destination "backup/stt_records_$(Get-Date -Format 'yyyyMMdd')" -Recurse
```

### 清理旧记录

通过Web界面逐个删除，或直接删除文件：
```bash
# 删除30天前的记录（谨慎操作！）
Get-ChildItem "stt_records\*.txt" | Where-Object {$_.LastWriteTime -lt (Get-Date).AddDays(-30)} | Remove-Item
```

## 常见问题

### Q: 文本没有实时更新？
A: 检查浏览器控制台是否有错误，刷新页面重试。确认自动刷新已开启。

### Q: 文件是否会很大？
A: 不会。纯文本压缩率高，一学期的记录可能只有几MB。

### Q: 可以关闭记录功能吗？
A: 可以，在 `config.json` 中设置 `"enable_stt_recording": false`，然后重启。

### Q: 记录会影响性能吗？
A: 几乎不会。文件I/O操作非常快，对识别性能无明显影响。

### Q: 文本文件乱码？
A: 使用支持UTF-8的编辑器打开（如VS Code、Notepad++）。Windows记事本需要选择UTF-8编码。

### Q: 能导入到其他软件吗？
A: 可以。文本文件是通用格式，可以导入Word、Excel、Notion等任何支持文本的软件。

## 技术细节

### 实时写入机制
```python
# 每次识别到文本后立即写入
self.current_text_file.write(f"[{time_str}] {text}\n")
self.current_text_file.flush()  # 立即刷新到磁盘
```

### 线程安全
- 写入操作在主线程执行
- 使用文件锁避免并发写入问题

### 字符编码
- 统一使用 UTF-8 编码
- 支持中文、英文及各种符号

## 未来计划

- [ ] 支持搜索功能（在所有记录中搜索关键词）
- [ ] 支持导出为PDF/Word格式
- [ ] 支持自动分段（按主题/时间）
- [ ] 支持语音回放与文本对照
- [ ] 支持标注和高亮

## 反馈

如有问题或建议，请提Issue: https://github.com/wangzhengzhuo05/realtimeBrowserSoundDetector/issues
